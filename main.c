#include "push_swap.h"

static int	check_all_digit(char *str)
{
	while (*str)
	{
		if (!ft_isdigit(*str))
			return (0);
		str++;
	}
	return (1);
}

int			main(int argc, char *argv[])
{
	if (argc == 1)
		return 

}